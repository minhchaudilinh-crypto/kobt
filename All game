-- ===== DỊCH VỤ =====
local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local RS = game:GetService("RunService")
local player = Players.LocalPlayer
local cam = workspace.CurrentCamera

-- ===== GUI =====
local sg = Instance.new("ScreenGui")
sg.Name = "ChauGui"
sg.ResetOnSpawn = false
sg.IgnoreGuiInset = true
sg.Parent = player:WaitForChild("PlayerGui")

-- ⚠️ THAY SỐ NÀY BẰNG ID ẢNH CỦA BẠN SAU KHI UPLOAD LÊN ROBLOX
local ANH_ID = "rbxassetid://0"   -- VD: "rbxassetid://1234567890"

-- ===== MẬT KHẨU =====
local MK = "menu lam boi matcuoi290"

local pf = Instance.new("Frame", sg)
pf.Size = UDim2.new(0, 260, 0, 150)
pf.Position = UDim2.new(0.5, -130, 0.5, -75)
pf.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
pf.Active = true
pf.Draggable = true
Instance.new("UICorner", pf).CornerRadius = UDim.new(0, 12)

local pt = Instance.new("TextLabel", pf)
pt.Size = UDim2.new(1, 0, 0, 35)
pt.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
pt.Text = "🔒 NHẬP MẬT KHẨU"
pt.TextColor3 = Color3.fromRGB(255, 255, 255)
pt.Font = Enum.Font.GothamBold
pt.TextSize = 15
Instance.new("UICorner", pt).CornerRadius = UDim.new(0, 12)

local pb = Instance.new("TextBox", pf)
pb.Size = UDim2.new(0.85, 0, 0, 32)
pb.Position = UDim2.new(0.075, 0, 0, 45)
pb.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
pb.PlaceholderText = "Nhập mật khẩu..."
pb.Text = ""
pb.TextColor3 = Color3.fromRGB(255, 255, 255)
pb.Font = Enum.Font.Gotham
pb.TextSize = 13
Instance.new("UICorner", pb).CornerRadius = UDim.new(0, 8)

local tb = Instance.new("TextLabel", pf)
tb.Size = UDim2.new(1, 0, 0, 22)
tb.Position = UDim2.new(0, 0, 0, 80)
tb.BackgroundTransparency = 1
tb.Text = ""
tb.TextColor3 = Color3.fromRGB(255, 60, 60)
tb.Font = Enum.Font.GothamBold
tb.TextSize = 13

local cb = Instance.new("TextButton", pf)
cb.Size = UDim2.new(0.85, 0, 0, 32)
cb.Position = UDim2.new(0.075, 0, 0, 105)
cb.BackgroundColor3 = Color3.fromRGB(60, 200, 100)
cb.Text = "✅ XÁC NHẬN"
cb.TextColor3 = Color3.fromRGB(255, 255, 255)
cb.Font = Enum.Font.GothamBold
cb.TextSize = 14
Instance.new("UICorner", cb).CornerRadius = UDim.new(0, 8)

-- ===== MENU CHÍNH =====
local mf = Instance.new("Frame", sg)
mf.Size = UDim2.new(0, 340, 0, 380)
mf.Position = UDim2.new(0.5, -170, 0.5, -190)
mf.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
mf.Active = true
mf.Draggable = true
mf.Visible = false
Instance.new("UICorner", mf).CornerRadius = UDim.new(0, 12)

-- ẢNH BANNER (nếu có ID)
if ANH_ID ~= "rbxassetid://0" then
    local banner = Instance.new("ImageLabel", mf)
    banner.Size = UDim2.new(1, 0, 0, 80)
    banner.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
    banner.Image = ANH_ID
    banner.ScaleType = Enum.ScaleType.Crop
    banner.Parent = mf
    Instance.new("UICorner", banner).CornerRadius = UDim.new(0, 12)
end

-- Tiêu đề
local mtitle = Instance.new("TextLabel", mf)
mtitle.Size = UDim2.new(1, -70, 0, 28)
mtitle.Position = UDim2.new(0, 0, 0, 0)
mtitle.BackgroundTransparency = 1
mtitle.Text = "⚡ Châu ⚡"
mtitle.TextColor3 = Color3.fromRGB(255, 255, 255)
mtitle.Font = Enum.Font.GothamBold
mtitle.TextSize = 15
mtitle.TextStrokeTransparency = 0

-- ➖ Thu gọn
local minBtn = Instance.new("TextButton", mf)
minBtn.Size = UDim2.new(0, 30, 0, 28)
minBtn.Position = UDim2.new(1, -65, 0, 0)
minBtn.BackgroundColor3 = Color3.fromRGB(200, 150, 60)
minBtn.Text = "➖"
minBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
minBtn.Font = Enum.Font.GothamBold
minBtn.TextSize = 16
Instance.new("UICorner", minBtn).CornerRadius = UDim.new(0, 8)

-- ❌ Tắt
local closeBtn = Instance.new("TextButton", mf)
closeBtn.Size = UDim2.new(0, 30, 0, 28)
closeBtn.Position = UDim2.new(1, -32, 0, 0)
closeBtn.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
closeBtn.Text = "❌"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 14
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 8)

-- ===== 3 TAB =====
local tabY = 35
local function makeTab(text, x)
    local b = Instance.new("TextButton", mf)
    b.Size = UDim2.new(0.3, 0, 0, 26)
    b.Position = UDim2.new(x, 0, 0, tabY)
    b.BackgroundColor3 = Color3.fromRGB(60, 60, 90)
    b.Text = text
    b.TextColor3 = Color3.fromRGB(255, 255, 255)
    b.Font = Enum.Font.GothamBold
    b.TextSize = 11
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 8)
    return b
end

local tabMain = makeTab("🏠 MAIN", 0.02)
local tabTP = makeTab("📡 TP", 0.35)
local tabESP = makeTab("👁️ ESP", 0.68)

local function makePage()
    local p = Instance.new("Frame", mf)
    p.Size = UDim2.new(1, 0, 1, -67)
    p.Position = UDim2.new(0, 0, 0, 67)
    p.BackgroundTransparency = 1
    p.Visible = false
    return p
end

local pageMain = makePage()
local pageTP = makePage()
local pageESP = makePage()
pageMain.Visible = true

local function switchTab(n)
    pageMain.Visible = (n == 1)
    pageTP.Visible = (n == 2)
    pageESP.Visible = (n == 3)
    tabMain.BackgroundColor3 = (n == 1) and Color3.fromRGB(60, 200, 100) or Color3.fromRGB(60, 60, 90)
    tabTP.BackgroundColor3 = (n == 2) and Color3.fromRGB(60, 200, 100) or Color3.fromRGB(60, 60, 90)
    tabESP.BackgroundColor3 = (n == 3) and Color3.fromRGB(60, 200, 100) or Color3.fromRGB(60, 60, 90)
end

tabMain.MouseButton1Click:Connect(function() switchTab(1) end)
tabTP.MouseButton1Click:Connect(function() switchTab(2); updatePlayerList() end)
tabESP.MouseButton1Click:Connect(function() switchTab(3) end)

-- ===== NÚT THU NHỎ ĐẸP (60x60 tròn, có icon) =====
local miniBtn = Instance.new("TextButton", sg)
miniBtn.Size = UDim2.new(0, 60, 0, 60)
miniBtn.Position = UDim2.new(0, 20, 0.5, -30)
miniBtn.BackgroundColor3 = Color3.fromRGB(60, 120, 200)
miniBtn.Text = "⚡"
miniBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
miniBtn.Font = Enum.Font.GothamBold
miniBtn.TextSize = 26
miniBtn.Visible = false
miniBtn.Active = true
miniBtn.Draggable = true
Instance.new("UICorner", miniBtn).CornerRadius = UDim.new(1, 0)   -- Tròn hoàn toàn

local miniStroke = Instance.new("UIStroke", miniBtn)
miniStroke.Color = Color3.fromRGB(100, 180, 255)
miniStroke.Thickness = 2

local miniGradient = Instance.new("UIGradient", miniBtn)
miniGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(60, 150, 220)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(100, 80, 200))
}
miniGradient.Rotation = 45

-- Hiệu ứng hover
miniBtn.MouseEnter:Connect(function()
    miniBtn:TweenSize(UDim2.new(0, 66, 0, 66), "Out", "Quad", 0.15)
end)
miniBtn.MouseLeave:Connect(function()
    miniBtn:TweenSize(UDim2.new(0, 60, 0, 60), "Out", "Quad", 0.15)
end)

minBtn.MouseButton1Click:Connect(function()
    mf.Visible = false
    miniBtn.Visible = true
end)

miniBtn.MouseButton1Click:Connect(function()
    mf.Visible = true
    miniBtn.Visible = false
end)

closeBtn.MouseButton1Click:Connect(function()
    sg:Destroy()
end)

-- =====================================================
-- ===== TAB MAIN: SPEED + FLY =====
-- =====================================================
local speedBtn = Instance.new("TextButton", pageMain)
speedBtn.Size = UDim2.new(0.9, 0, 0, 32)
speedBtn.Position = UDim2.new(0.05, 0, 0, 5)
speedBtn.BackgroundColor3 = Color3.fromRGB(60, 200, 100)
speedBtn.Text = "⚡ SPEED: TẮT"
speedBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
speedBtn.Font = Enum.Font.GothamBold
speedBtn.TextSize = 13
Instance.new("UICorner", speedBtn).CornerRadius = UDim.new(0, 8)

local speedBox = Instance.new("TextBox", pageMain)
speedBox.Size = UDim2.new(0.9, 0, 0, 28)
speedBox.Position = UDim2.new(0.05, 0, 0, 40)
speedBox.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
speedBox.Text = "100"
speedBox.TextColor3 = Color3.fromRGB(255, 255, 255)
speedBox.Font = Enum.Font.Gotham
speedBox.TextSize = 12
Instance.new("UICorner", speedBox).CornerRadius = UDim.new(0, 8)

local flyBtn = Instance.new("TextButton", pageMain)
flyBtn.Size = UDim2.new(0.9, 0, 0, 32)
flyBtn.Position = UDim2.new(0.05, 0, 0, 75)
flyBtn.BackgroundColor3 = Color3.fromRGB(60, 120, 200)
flyBtn.Text = "🕊️ BAY: TẮT"
flyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
flyBtn.Font = Enum.Font.GothamBold
flyBtn.TextSize = 13
Instance.new("UICorner", flyBtn).CornerRadius = UDim.new(0, 8)

local flySpeedBox = Instance.new("TextBox", pageMain)
flySpeedBox.Size = UDim2.new(0.9, 0, 0, 28)
flySpeedBox.Position = UDim2.new(0.05, 0, 0, 110)
flySpeedBox.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
flySpeedBox.Text = "50"
flySpeedBox.TextColor3 = Color3.fromRGB(255, 255, 255)
flySpeedBox.Font = Enum.Font.Gotham
flySpeedBox.TextSize = 12
Instance.new("UICorner", flySpeedBox).CornerRadius = UDim.new(0, 8)

local hint = Instance.new("TextLabel", pageMain)
hint.Size = UDim2.new(0.9, 0, 0, 80)
hint.Position = UDim2.new(0.05, 0, 0, 145)
hint.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
hint.Text = "🎮 BAY:\n• Mobile: 6 nút góc phải\n• PC: W/A/S/D + Space/Shift"
hint.TextColor3 = Color3.fromRGB(200, 200, 200)
hint.Font = Enum.Font.Gotham
hint.TextSize = 11
hint.TextWrapped = true
Instance.new("UICorner", hint).CornerRadius = UDim.new(0, 8)

-- ===== NÚT BAY NGOÀI =====
local fc = Instance.new("Frame", sg)
fc.Size = UDim2.new(0, 170, 0, 170)
fc.Position = UDim2.new(1, -180, 1, -180)
fc.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
fc.BackgroundTransparency = 0.3
fc.Visible = false
fc.Active = true
fc.Draggable = true
Instance.new("UICorner", fc).CornerRadius = UDim.new(0, 12)

local dirs = {
    {name="⬆️", x=0.5, y=0.02, dir="up"},
    {name="⬅️", x=0.05, y=0.35, dir="left"},
    {name="🔼", x=0.5, y=0.35, dir="forward"},
    {name="➡️", x=0.95, y=0.35, dir="right"},
    {name="⬇️", x=0.5, y=0.68, dir="down"},
    {name="🔽", x=0.5, y=0.95, dir="back"},
}
local flyDirs = {forward=false, back=false, left=false, right=false, up=false, down=false}

for _, d in ipairs(dirs) do
    local b = Instance.new("TextButton", fc)
    b.Size = UDim2.new(0, 45, 0, 45)
    b.Position = UDim2.new(d.x, -22, d.y, -22)
    b.BackgroundColor3 = Color3.fromRGB(70, 100, 180)
    b.Text = d.name
    b.TextColor3 = Color3.fromRGB(255, 255, 255)
    b.Font = Enum.Font.GothamBold
    b.TextSize = 18
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 8)
    b.MouseButton1Down:Connect(function() flyDirs[d.dir] = true end)
    b.MouseButton1Up:Connect(function() flyDirs[d.dir] = false end)
    b.MouseLeave:Connect(function() flyDirs[d.dir] = false end)
end

-- ===== SPEED + FLY (bản thân) =====
local isSpeed, oldSpeed, speedConn = false, 16, nil

speedBtn.MouseButton1Click:Connect(function()
    isSpeed = not isSpeed
    if isSpeed then
        speedBtn.Text = "⚡ SPEED: BẬT"
        speedBtn.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
        local h = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if h then oldSpeed = h.WalkSpeed end
        speedConn = RS.Heartbeat:Connect(function()
            local c = player.Character
            if c then
                local h = c:FindFirstChildOfClass("Humanoid")
                if h then h.WalkSpeed = tonumber(speedBox.Text) or 100 end
            end
        end)
    else
        speedBtn.Text = "⚡ SPEED: TẮT"
        speedBtn.BackgroundColor3 = Color3.fromRGB(60, 200, 100)
        if speedConn then speedConn:Disconnect(); speedConn = nil end
        local h = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if h then h.WalkSpeed = oldSpeed end
    end
end)

local isFly, flyConn, bv = false, nil, nil

flyBtn.MouseButton1Click:Connect(function()
    isFly = not isFly
    if isFly then
        flyBtn.Text = "🕊️ BAY: BẬT"
        flyBtn.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
        fc.Visible = true
        local c = player.Character
        if not c then isFly = false; fc.Visible = false; return end
        local hrp = c:FindFirstChild("HumanoidRootPart")
        if not hrp then isFly = false; fc.Visible = false; return end
        bv = Instance.new("BodyVelocity")
        bv.MaxForce = Vector3.new(9e9, 9e9, 9e9)
        bv.Velocity = Vector3.new(0, 0, 0)
        bv.Parent = hrp
        flyConn = RS.RenderStepped:Connect(function()
            if not bv or not bv.Parent then return end
            local sp = tonumber(flySpeedBox.Text) or 50
            local dir = Vector3.new(0, 0, 0)
            local look = cam.CFrame.LookVector
            local right = cam.CFrame.RightVector
            if flyDirs.forward then dir = dir + look end
            if flyDirs.back then dir = dir - look end
            if flyDirs.right then dir = dir + right end
            if flyDirs.left then dir = dir - right end
            if flyDirs.up then dir = dir + Vector3.new(0, 1, 0) end
            if flyDirs.down then dir = dir - Vector3.new(0, 1, 0) end
            if UIS:IsKeyDown(Enum.KeyCode.W) then dir = dir + look end
            if UIS:IsKeyDown(Enum.KeyCode.S) then dir = dir - look end
            if UIS:IsKeyDown(Enum.KeyCode.A) then dir = dir - right end
            if UIS:IsKeyDown(Enum.KeyCode.D) then dir = dir + right end
            if UIS:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0, 1, 0) end
            if UIS:IsKeyDown(Enum.KeyCode.LeftShift) then dir = dir - Vector3.new(0, 1, 0) end
            bv.Velocity = dir.Magnitude > 0 and dir.Unit * sp or Vector3.new(0, 0, 0)
        end)
    else
        flyBtn.Text = "🕊️ BAY: TẮT"
        flyBtn.BackgroundColor3 = Color3.fromRGB(60, 120, 200)
        fc.Visible = false
        if flyConn then flyConn:Disconnect(); flyConn = nil end
        if bv then bv:Destroy(); bv = nil end
        for k, _ in pairs(flyDirs) do flyDirs[k] = false end
    end
end)

-- =====================================================
-- ===== TAB TP: TP + SPEED + BAY NGƯỜI KHÁC =====
-- =====================================================
local playerList = Instance.new("ScrollingFrame", pageTP)
playerList.Size = UDim2.new(0.9, 0, 0, 250)
playerList.Position = UDim2.new(0.05, 0, 0, 10)
playerList.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
playerList.BorderSizePixel = 0
playerList.ScrollBarThickness = 6
playerList.CanvasSize = UDim2.new(0, 0, 0, 0)
playerList.AutomaticCanvasSize = Enum.AutomaticSize.Y
Instance.new("UICorner", playerList).CornerRadius = UDim.new(0, 8)
Instance.new("UIListLayout", playerList).Padding = UDim.new(0, 4)
local lp = Instance.new("UIPadding", playerList)
lp.PaddingTop = UDim.new(0, 5)
lp.PaddingLeft = UDim.new(0, 5)

-- Lưu trạng thái các chức năng
local otherSpeedConns = {}
local otherFlyConns = {}
local otherFloatConns = {}

function updatePlayerList()
    for _, c in pairs(playerList:GetChildren()) do
        if c:IsA("Frame") then c:Destroy() end
    end
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= player then
            local row = Instance.new("Frame", playerList)
            row.Size = UDim2.new(1, -10, 0, 55)
            row.BackgroundColor3 = Color3.fromRGB(60, 60, 90)
            row.BorderSizePixel = 0
            Instance.new("UICorner", row).CornerRadius = UDim.new(0, 6)

            -- Tên
            local n = Instance.new("TextLabel", row)
            n.Size = UDim2.new(1, -10, 0, 18)
            n.Position = UDim2.new(0, 5, 0, 2)
            n.BackgroundTransparency = 1
            n.Text = "👤 " .. p.Name
            n.TextColor3 = Color3.fromRGB(255, 255, 255)
            n.Font = Enum.Font.GothamBold
            n.TextSize = 11
            n.TextXAlignment = Enum.TextXAlignment.Left

            -- Dòng nút
            local btnY = 22
            local btnW = 0.235
            local btnH = 26

            -- Nút ĐẾN
            local gb = Instance.new("TextButton", row)
            gb.Size = UDim2.new(btnW, 0, 0, btnH)
            gb.Position = UDim2.new(0.01, 0, 0, btnY)
            gb.BackgroundColor3 = Color3.fromRGB(60, 150, 200)
            gb.Text = "ĐẾN"
            gb.TextColor3 = Color3.fromRGB(255, 255, 255)
            gb.Font = Enum.Font.GothamBold
            gb.TextSize = 10
            Instance.new("UICorner", gb).CornerRadius = UDim.new(0, 5)

            -- Nút KÉO
            local bb = Instance.new("TextButton", row)
            bb.Size = UDim2.new(btnW, 0, 0, btnH)
            bb.Position = UDim2.new(0.255, 0, 0, btnY)
            bb.BackgroundColor3 = Color3.fromRGB(200, 100, 60)
            bb.Text = "KÉO"
            bb.TextColor3 = Color3.fromRGB(255, 255, 255)
            bb.Font = Enum.Font.GothamBold
            bb.TextSize = 10
            Instance.new("UICorner", bb).CornerRadius = UDim.new(0, 5)

            -- Nút SPEED
            local sb = Instance.new("TextButton", row)
            sb.Size = UDim2.new(btnW, 0, 0, btnH)
            sb.Position = UDim2.new(0.5, 0, 0, btnY)
            sb.BackgroundColor3 = Color3.fromRGB(60, 200, 100)
            sb.Text = "🏃"
            sb.TextColor3 = Color3.fromRGB(255, 255, 255)
            sb.Font = Enum.Font.GothamBold
            sb.TextSize = 12
            Instance.new("UICorner", sb).CornerRadius = UDim.new(0, 5)

            -- Nút BAY
            local fb = Instance.new("TextButton", row)
            fb.Size = UDim2.new(btnW, 0, 0, btnH)
            fb.Position = UDim2.new(0.745, 0, 0, btnY)
            fb.BackgroundColor3 = Color3.fromRGB(150, 80, 200)
            fb.Text = "🕊️"
            fb.TextColor3 = Color3.fromRGB(255, 255, 255)
            fb.Font = Enum.Font.GothamBold
            fb.TextSize = 12
            Instance.new("UICorner", fb).CornerRadius = UDim.new(0, 5)

            -- ĐẾN
            gb.MouseButton1Click:Connect(function()
                local mc, tc = player.Character, p.Character
                if mc and tc then
                    local mh = mc:FindFirstChild("HumanoidRootPart")
                    local th = tc:FindFirstChild("HumanoidRootPart")
                    if mh and th then mh.CFrame = th.CFrame + Vector3.new(0, 0, 3) end
                end
            end)

            -- KÉO
            bb.MouseButton1Click:Connect(function()
                local mc, tc = player.Character, p.Character
                if mc and tc then
                    local mh = mc:FindFirstChild("HumanoidRootPart")
                    local th = tc:FindFirstChild("HumanoidRootPart")
                    if mh and th then th.CFrame = mh.CFrame + Vector3.new(0, 0, 3) end
                end
            end)

            -- 🏃 CẤP SPEED (cách mới: dùng BodyVelocity liên tục)
            sb.MouseButton1Click:Connect(function()
                if otherSpeedConns[p] then
                    otherSpeedConns[p]:Disconnect()
                    otherSpeedConns[p] = nil
                    sb.BackgroundColor3 = Color3.fromRGB(60, 200, 100)
                    sb.Text = "🏃"
                else
                    sb.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
                    sb.Text = "✋"
                    -- Cách mới: dùng velocity của Humanoid + CFrame
                    otherSpeedConns[p] = RS.Heartbeat:Connect(function()
                        local c = p.Character
                        if not c then return end
                        local h = c:FindFirstChildOfClass("Humanoid")
                        if h then h.WalkSpeed = 200 end
                        local hrp = c:FindFirstChild("HumanoidRootPart")
                        if hrp then
                            -- Đẩy nhẹ để server chấp nhận
                            local moveDir = h.MoveDirection
                            if moveDir.Magnitude > 0 then
                                hrp.Velocity = moveDir * 200
                            end
                        end
                    end)
                end
            end)

            -- 🕊️ LÀM BAY
            fb.MouseButton1Click:Connect(function()
                if otherFlyConns[p] then
                    -- Đang bay → tắt (rơi xuống)
                    otherFlyConns[p]:Disconnect()
                    otherFlyConns[p] = nil
                    fb.BackgroundColor3 = Color3.fromRGB(150, 80, 200)
                    fb.Text = "🕊️"
                    local c = p.Character
                    if c then
                        local hrp = c:FindFirstChild("HumanoidRootPart")
                        if hrp then
                            local bvOld = hrp:FindFirstChild("ChauFlyBV")
                            if bvOld then bvOld:Destroy() end
                        end
                    end
                else
                    -- Bật bay
                    fb.BackgroundColor3 = Color3.fromRGB(255, 100, 100)
                    fb.Text = "✋"
                    local c = p.Character
                    if not c then return end
                    local hrp = c:FindFirstChild("HumanoidRootPart")
                    if not hrp then return end

                    -- Tạo BodyVelocity trong nhân vật họ
                    local theirBv = Instance.new("BodyVelocity")
                    theirBv.Name = "ChauFlyBV"
                    theirBv.MaxForce = Vector3.new(9e9, 9e9, 9e9)
                    theirBv.Velocity = Vector3.new(0, 30, 0)   -- Bay LÊN
                    theirBv.Parent = hrp

                    -- Duy trì bay lên + lơ lửng
                    otherFlyConns[p] = RS.Heartbeat:Connect(function()
                        local cc = p.Character
                        if not cc then return end
                        local hh = cc:FindFirstChild("HumanoidRootPart")
                        if not hh then return end
                        local b = hh:FindFirstChild("ChauFlyBV")
                        if not b then
                            -- Tạo lại nếu bị xoá
                            b = Instance.new("BodyVelocity")
                            b.Name = "ChauFlyBV"
                            b.MaxForce = Vector3.new(9e9, 9e9, 9e9)
                            b.Parent = hh
                        end
                        -- Lơ lửng: đẩy lên nhẹ để không rơi
                        b.Velocity = Vector3.new(0, 25, 0)
                    end)
                end
            end)
        end
    end
end

updatePlayerList()
Players.PlayerAdded:Connect(function() task.wait(1) updatePlayerList() end)
Players.PlayerRemoving:Connect(function(p)
    if otherSpeedConns[p] then otherSpeedConns[p]:Disconnect(); otherSpeedConns[p] = nil end
    if otherFlyConns[p] then otherFlyConns[p]:Disconnect(); otherFlyConns[p] = nil end
    if otherFloatConns[p] then otherFloatConns[p]:Disconnect(); otherFloatConns[p] = nil end
    updatePlayerList()
end)

-- =====================================================
-- ===== TAB ESP =====
-- =====================================================
local espBtn = Instance.new("TextButton", pageESP)
espBtn.Size = UDim2.new(0.9, 0, 0, 35)
espBtn.Position = UDim2.new(0.05, 0, 0, 10)
espBtn.BackgroundColor3 = Color3.fromRGB(60, 200, 100)
espBtn.Text = "👁️ ESP: TẮT"
espBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
espBtn.Font = Enum.Font.GothamBold
espBtn.TextSize = 13
Instance.new("UICorner", espBtn).CornerRadius = UDim.new(0, 8)

local espActive, espObjects = false, {}

local function createESP(p)
    if not p.Character then return end
    espObjects[p] = {}
    local hl = Instance.new("Highlight")
    hl.FillColor = Color3.fromRGB(255, 0, 0)
    hl.OutlineColor = Color3.fromRGB(255, 255, 255)
    hl.FillTransparency = 0.5
    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    hl.Parent = p.Character
    table.insert(espObjects[p], hl)

    local bb = Instance.new("BillboardGui")
    bb.Size = UDim2.new(0, 150, 0, 40)
    bb.StudsOffset = Vector3.new(0, 3, 0)
    bb.AlwaysOnTop = true
    bb.Parent = p.Character:FindFirstChild("Head") or p.Character

    local nm = Instance.new("TextLabel", bb)
    nm.Size = UDim2.new(1, 0, 0, 20)
    nm.BackgroundTransparency = 1
    nm.Text = p.Name
    nm.TextColor3 = Color3.fromRGB(255, 255, 255)
    nm.TextStrokeTransparency = 0
    nm.Font = Enum.Font.GothamBold
    nm.TextSize = 13

    local dl = Instance.new("TextLabel", bb)
    dl.Size = UDim2.new(1, 0, 0, 18)
    dl.Position = UDim2.new(0, 0, 0, 20)
    dl.BackgroundTransparency = 1
    dl.Text = "..."
    dl.TextColor3 = Color3.fromRGB(255, 255, 100)
    dl.TextStrokeTransparency = 0
    dl.Font = Enum.Font.GothamBold
    dl.TextSize = 11

    table.insert(espObjects[p], bb)

    local conn = RS.RenderStepped:Connect(function()
        if not espActive then return end
        if not p.Character or not player.Character then return end
        local a = p.Character:FindFirstChild("HumanoidRootPart")
        local b = player.Character:FindFirstChild("HumanoidRootPart")
        if a and b then dl.Text = math.floor((a.Position - b.Position).Magnitude) .. " studs" end
    end)
    table.insert(espObjects[p], {Disconnect = function() conn:Disconnect() end})
end

local function removeESP(p)
    if espObjects[p] then
        for _, o in pairs(espObjects[p]) do
            if type(o) == "table" and o.Disconnect then o:Disconnect()
            elseif o and o.Parent then o:Destroy() end
        end
        espObjects[p] = nil
    end
end

local function updateAllESP()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= player then
            if espActive then createESP(p) else removeESP(p) end
        end
    end
end

espBtn.MouseButton1Click:Connect(function()
    espActive = not espActive
    espBtn.Text = espActive and "👁️ ESP: BẬT" or "👁️ ESP: TẮT"
    espBtn.BackgroundColor3 = espActive and Color3.fromRGB(200, 60, 60) or Color3.fromRGB(60, 200, 100)
    updateAllESP()
end)

Players.PlayerAdded:Connect(function(p)
    p.CharacterAdded:Connect(function()
        task.wait(1)
        if espActive then createESP(p) end
    end)
end)
Players.PlayerRemoving:Connect(removeESP)

-- =====================================================
-- ===== XÁC NHẬN MẬT KHẨU =====
-- =====================================================
cb.MouseButton1Click:Connect(function()
    if pb.Text == MK then
        pf.Visible = false
        mf.Visible = true
        updatePlayerList()
    else
        tb.Text = "a con chó"
        pb.Text = ""
    end
end)

pb.FocusLost:Connect(function(enter)
    if enter then cb:Activate() end
end)

-- ===== TỰ TẮT KHI HỒI SINH =====
player.CharacterAdded:Connect(function()
    isSpeed, isFly = false, false
    if speedConn then speedConn:Disconnect(); speedConn = nil end
    if flyConn then flyConn:Disconnect(); flyConn = nil end
    if bv then bv:Destroy(); bv = nil end
    fc.Visible = false
    speedBtn.Text = "⚡ SPEED: TẮT"
    speedBtn.BackgroundColor3 = Color3.fromRGB(60, 200, 100)
    flyBtn.Text = "🕊️ BAY: TẮT"
    flyBtn.BackgroundColor3 = Color3.fromRGB(60, 120, 200)
end)
